# Sprint-Log

```yaml
sprint:
  schwerpunkt: Sprachliche Systemgrenzen und Agentenbenennung festigen
  ziel: OpenClaw Nexus verwendet eine klare, nicht überladene Begrifflichkeit: Nexus bleibt Projekt-/Authority-Kontext, nexusctl bleibt autoritative Control-Software, OpenClaw bleibt Runtime, und handelnde Rollen sind als Agents benannt.
  nicht_ziele:
    - Vollständige Umbenennung des Projekts OpenClaw Nexus.
    - Umbenennung von nexusctl.
    - Umbenennung des Ordners nexus/ oder erzwungene Migration auf neue Dateipfade.
    - Einführung einer echten OpenClaw-Produktionsruntime.
    - Implementierung eines vollständigen Schedule-Change-Request-Produkts außerhalb der Naming-/Contract-Härtung.
  validierungsniveau: fast
  ausgabe: openclaw-nexus.zip
  aktuelle_phase: abgeschlossen
```

## P1 — Begriffskontrakt in aktiver Dokumentation festziehen

Ziel:
- Die aktive Dokumentation beschreibt eindeutig, welche Systemebene welchen Namen trägt und welche Begriffe nicht mehr verwendet werden sollen.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `README.md`
- `docs/product/overview.md`
- `docs/operations/internal-production.md`
- `.chatgpt/state/CURRENT_STATE.md` nur als abgeschlossener Zustand, nicht als Live-Log
- Bestehende Projektbegriffe aus `nexus/blueprint.yml`

Aufgaben:
- Einen kompakten Sprachkontrakt in der aktiven Produktdokumentation ergänzen.
- Festhalten: `OpenClaw Nexus` ist Projekt-/Architekturkontext, `nexusctl` ist die autoritative Control-Software, `Control Config` beschreibt den Soll-Zustand, `Control Store` hält State/Events, `Policy Engine` entscheidet Regeln/Gates, `OpenClaw Runtime Config` ist generiert, `OpenClaw Runtime` führt aus, `Agents` handeln.
- Explizit vermeiden: tief verwurzelte Komponentennamen wie `Nexus Core`, `Nexus Ledger`, `Nexus Blueprint`, `Nexus Scheduler` als offizielles internes Namensschema.
- Die Source-of-Truth-Regel dokumentieren: alle autoritativen Änderungen laufen über `nexusctl`; Agents dürfen Requests/Proposals erzeugen und erlaubte Schritte ausführen, aber nicht direkt autoritativen State mutieren.
- Prüfen, ob aktive Dokumentation den bisherigen Agenten `nexus` weiterhin als Authority missverständlich beschreibt, und die Stellen für P2 markieren oder bereits sprachlich entschärfen, ohne IDs zu ändern.

Akzeptanzkriterien:
- Die aktive Dokumentation enthält eine eindeutige Begriffstabelle oder einen kompakten Architekturvertrag.
- Die Dokumentation trennt Projektkontext, Control-Software, Runtime und Agents klar.
- Der Nexus-Bezug bleibt sichtbar, aber wird nicht als tiefes technisches Prefix-Schema ausgebaut.
- Keine aktive Doku behauptet, dass ein Agent selbst die Source of Truth ist.
- Keine historischen Archivdateien werden als aktive Wahrheit angepasst oder genutzt.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast`

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `README.md`
  - `docs/product/overview.md`
  - `docs/product/local-development.md`
  - `docs/operations/internal-production.md`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `./scripts/run_tests.sh fast` — bestanden, 42 Tests, 103 abgewählt
- Offene Punkte:
  - Agent-Display-Names und die mögliche ID-Migration von `nexus`/`merge-applier` bleiben für P2 offen.

## P2 — Agentennamen und Control-Rollen konsistent machen

Ziel:
- Handelnde OpenClaw-Rollen sind im System als Agents erkennbar, ohne dass jeder Agent `OpenClaw` oder `Nexus` im Namen tragen muss.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `nexus/agents.yml`
- `nexus/standing-orders.yml`
- `nexus/schedules.yml`
- `nexus/capabilities.yml`
- `nexus/policies.yml`
- `scripts/validate_project.py`
- Tests mit Agent-IDs und Login-Flows
- Generierte Artefakte unter `generated/agents`, `generated/skills`, `generated/openclaw`

Aufgaben:
- Display Names der handelnden Rollen auf das Muster `<Funktion> Agent` bringen, z. B. `Software Builder Agent`, `Trading Strategist Agent`, `Control Router Agent`, `Merge Applier Agent`.
- Den bisherigen Agenten `nexus` fachlich zu `control-router` migrieren, sofern keine harte technische Blockade entsteht.
- Den bisherigen Agenten `merge-applier` fachlich zu `merge-applier` migrieren, sofern keine harte technische Blockade entsteht.
- Rollenbeschreibungen so anpassen, dass `control-router` Requests routed und `merge-applier` nur Gate-konform anwendet, aber beide nicht selbst die Authority sind.
- Validator-Erwartungen, Standing Orders, Schedules, Tests und generierte Artefakte an die neuen Agent-IDs anpassen.
- Falls eine direkte ID-Migration in einer Phase zu breit wird, mindestens Display Names und Beschreibungen korrigieren und die ID-Migration als offenen Punkt dokumentieren.

Akzeptanzkriterien:
- Alle aktiven Agent Display Names enden mit `Agent`, außer bewusst ausgeschlossene technische Sonderrollen, die begründet dokumentiert sind.
- Es gibt keinen aktiven Display Name `Nexus`, der wie die Authority selbst wirkt.
- Control-Router- und Merge-Applier-Verantwortung sind sprachlich getrennt.
- Validator und Fast-Tests akzeptieren die neue Agentenbenennung oder dokumentieren einen bewusst verschobenen ID-Migrationspunkt.
- Generierte Runtime-Artefakte sind nach der Änderung neu erzeugt und tragen konsistente Agentennamen.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast`
- bei ID-Migration zusätzlich fokussiert: Tests für Auth, Scopes, Merge Gate, Schedules und OpenClaw-Generierung

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `nexus/agents.yml`
  - `nexus/standing-orders.yml`
  - `nexus/schedules.yml`
  - `nexus/policies.yml`
  - `scripts/validate_project.py`
  - `nexusctl/src/nexusctl/authz/policy_engine.py`
  - `nexusctl/src/nexusctl/app/merge_service.py`
  - `nexusctl/src/nexusctl/app/schedule_service.py`
  - `tests/test_blueprint_contract.py`
  - `tests/test_auth_identity.py`
  - `tests/test_policy_contract.py`
  - `tests/test_work_scopes.py`
  - `tests/test_merge_gate.py`
  - `tests/test_runtime_tool_contract.py`
  - weitere betroffene CLI-/GitHub-/Policy-/Review-/E2E-Contract-Tests mit Agent-ID-Referenzen
  - `generated/agents/control-router/*`
  - `generated/agents/merge-applier/*`
  - `generated/skills/allowlists/control-router.json`
  - `generated/skills/allowlists/merge-applier.json`
  - `generated/skills/tool-policies/control-router.json`
  - `generated/skills/tool-policies/merge-applier.json`
  - `generated/openclaw/openclaw.json`
  - `generated/openclaw/schedules/control_router_domain_inbox_triage.json`
  - `generated/openclaw/schedules/control_router_scope_expiry_guard.json`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python -m pytest --basetemp /tmp/openclaw-nexus-pytest -m 'not integration and not slow and not timeout_risk' -q --durations=25` — bestanden, 42 Tests, 103 abgewählt
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_auth_identity.py -q --durations=10` — bestanden, 6 Tests
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_work_scopes.py -q --durations=10` — bestanden, 4 Tests
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_merge_gate.py -q --durations=10` — bestanden, 2 Tests
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_schedules.py -q --durations=10` — bestanden, 1 Test
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/test_openclaw_generation.py -q --durations=10` — bestanden, 2 Tests
  - Hinweis: `./scripts/run_tests.sh fast` zeigte mit aktiven externen Pytest-Plugins nach 100% Testfortschritt einen Harness-Timeout; der plugin-deaktivierte äquivalente Fast-Lauf lief vollständig durch.
- Offene Punkte:
  - P3 bleibt offen für die zusätzliche Authority-Härtung rund um Config, Schedules und Runtime.

## P3 — Authority-Grenzen für Config, Schedules und Runtime schärfen

Ziel:
- Das System macht prüfbar sichtbar, dass Agents Konfiguration, Cronjobs/Schedules und Runtime-Artefakte nicht direkt als Source of Truth verändern, sondern über `nexusctl` gehen.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `nexus/blueprint.yml`
- `nexus/policies.yml`
- `nexus/capabilities.yml`
- `nexus/schedules.yml`
- `nexus/runtime-tools.yml`
- `nexusctl/src/nexusctl/authz/policy_engine.py`
- `nexusctl/src/nexusctl/app/schedule_service.py`
- `tests/test_policy_contract.py`
- `tests/test_schedules.py`
- `tests/test_runtime_tool_contract.py`
- `tests/test_architecture_contracts.py`

Aufgaben:
- Den Architekturvertrag für autoritative Mutationen präzisieren: Control Config und Control Store sind nur über `nexusctl` beziehungsweise geprüfte Services zu verändern.
- Schedule-/Cron-Verwaltung als Nexusctl-/Control-Flow dokumentieren: Agents dürfen Schedule-Wünsche formulieren oder erlaubte Runs auslösen, aber nicht direkt Runtime-Cronjobs ändern.
- Bestehende Policies und Tests prüfen, ob sie direkte Agent-GitHub-Writes, Builder-Repo-Apply und Generated-Artefakt-Manual-Edits bereits absichern.
- Bei Bedarf Contract-Tests ergänzen, die die neue Begriffs- und Authority-Regel absichern, ohne ein vollständiges Schedule-Change-Request-Produkt zu bauen.
- Prüfen, ob `openclaw` in `nexus/blueprint.yml` sprachlich korrekt als Runtime beschrieben ist und ob `configuration: generated_from_nexus` besser als generierte OpenClaw Runtime Config erklärt werden sollte.

Akzeptanzkriterien:
- Der Authority-Vertrag ist in Config/Doku/Tests konsistent: Agents handeln, `nexusctl` entscheidet und schreibt.
- Schedules/Cronjobs sind als über `nexusctl` kontrollierter Zustand beschrieben, nicht als direkt mutierbare OpenClaw-Runtime-Freiheit.
- Bestehende harte Invarianten bleiben grün oder werden zielsprachlich präzisiert.
- Keine neuen Produktversprechen für noch nicht vorhandene Schedule-Change-Request-Funktionen entstehen.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast`
- fokussiert bei Teständerungen: `tests/test_policy_contract.py`, `tests/test_schedules.py`, `tests/test_runtime_tool_contract.py`, `tests/test_architecture_contracts.py`

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `docs/product/overview.md`
  - `docs/product/local-development.md`
  - `docs/operations/internal-production.md`
  - `nexus/blueprint.yml`
  - `nexus/policies.yml`
  - `nexus/capabilities.yml`
  - `nexus/schedules.yml`
  - `nexus/runtime-tools.yml`
  - `nexusctl/src/nexusctl/app/schedule_service.py`
  - `nexusctl/src/nexusctl/adapters/openclaw/config_writer.py`
  - `nexusctl/src/nexusctl/adapters/openclaw/schedule_writer.py`
  - `scripts/validate_project.py`
  - `tests/test_blueprint_contract.py`
  - `tests/test_policy_contract.py`
  - `tests/test_runtime_tool_contract.py`
  - `tests/test_schedules.py`
  - `generated/openclaw/openclaw.json`
  - `generated/openclaw/schedules/*.json`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `./scripts/run_tests.sh fast` — Harness-Timeout nach begonnenem Fast-Lauf; gemäß Timeout-Policy nicht als Pytest-Fehler gewertet
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 python -m pytest --basetemp /tmp/openclaw-nexus-pytest -m 'not integration and not slow and not timeout_risk' -q --durations=25` — bestanden vor der reinen Doku-Nachschärfung, 44 Tests, 104 abgewählt; nach Doku-Nachschärfung beim Gesamtlauf erneut Harness-Timeout
  - Fokussiert bestanden: `tests/test_blueprint_contract.py`, `tests/test_policy_contract.py`, `tests/test_runtime_tool_contract.py`, `tests/test_schedules.py`, `tests/test_architecture_contracts.py`
  - Die übrigen Fast-Dateien wurden nach dem Timeout einzeln mit deaktiviertem Plugin-Autoload geprüft und bestanden: `tests/test_archive_policy.py`, `tests/test_cli_runtime.py`, `tests/test_github_projection.py`, `tests/test_target_version_contracts.py`, `tests/test_test_strategy.py`, `nexusctl/tests/unit/test_placeholder_structure.py`
- Offene Punkte:
  - P4 bleibt offen für die abschließende Synchronisierung von Status, Sprint-Delta und Übergabeartefakt.

## P4 — Runtime-Artefakte, Status und Abschluss sauber synchronisieren

Ziel:
- Die umgesetzte Terminologie ist in aktiver Doku, Config, Tests, generierten OpenClaw-Artefakten und abgeschlossenem Projektstatus synchron.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `generated/openclaw/openclaw.json`
- `generated/agents/*`
- `generated/skills/*`
- `.chatgpt/state/phases.md`
- `.chatgpt/state/CURRENT_STATE.md`
- `docs/archiv/sprints/`
- `scripts/package_project.py`
- `scripts/close_sprint.py`

Aufgaben:
- Alle generierten OpenClaw-Artefakte nach Config-/Agentenänderungen neu erzeugen.
- Prüfen, ob generierte Agent-Verzeichnisse, Allowlists, Tool-Policies und Schedule-Dateien die neue Benennung konsistent widerspiegeln.
- `Current-State-Delta` in `.chatgpt/state/phases.md` vollständig aktualisieren.
- Abschluss-Doublecheck vorbereiten: tatsächlichen Ist-Zustand aus Code, Tests, aktiver Doku, Scripts und Konfiguration gegen das Delta prüfen.
- Sprint mit `clear Sprint` abschließbar machen: Archivierung, Leeren von `phases.md`, Aktualisierung von `CURRENT_STATE.md` und ZIP-Übergabe.

Akzeptanzkriterien:
- `python scripts/validate_project.py` ist grün.
- `./scripts/run_tests.sh fast` ist grün oder dokumentiert nur bekannte Timeout-Risiko-Ausnahmen gemäß Sprint-Workflow.
- Generierte OpenClaw Runtime Config ist konsistent mit Control Config.
- `Current-State-Delta` beschreibt neue Fähigkeiten, geänderte Grenzen und offene Arbeiten realistisch.
- Der Sprint kann mit der bestehenden Clear-Up-Funktion sauber abgeschlossen werden.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast`
- optional fokussiert: `python -m nexusctl.interfaces.cli.main generate all --db <tmp-db> --project-root . --json`

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `.chatgpt/state/phases.md`
  - `generated/*` geprüft und via `nexusctl generate all` erneut abgeglichen; keine Inhaltsänderungen erforderlich
- Validierung:
  - `python -m nexusctl.interfaces.cli.main generate all --db <tmp-db> --project-root . --json` mit lokalem `platform-maintainer`-Token — bestanden, 136 Artefakte geprüft, `changed_count: 0`
  - `python scripts/validate_project.py` — bestanden
  - `./scripts/run_tests.sh fast` — bestanden, 44 Tests, 104 abgewählt
  - Aktive Alt-ID-Prüfung für `nexus`/`nexus-applier` in Agent-IDs, Display Names und generierten Agent-Verzeichnissen — bestanden; verbleibende Treffer sind Archivhistorie oder zulässige `nexusctl`-/Tool-IDs
- Offene Punkte:
  - Sprintabschluss per `clear Sprint` bleibt als separater Abschlussmodus offen; dabei sind `CURRENT_STATE.md`, Archivierung und Leeren von `phases.md` auszuführen.

## Current-State-Delta

Neue Fähigkeiten:
- Umgesetzt: Die aktive Produktdokumentation enthält einen klaren Sprach- und Architekturvertrag für OpenClaw Nexus, `nexusctl`, Control Config, Control Store, Policy Engine, OpenClaw Runtime Config, OpenClaw Runtime und Agents.
- Umgesetzt: Alle aktiven Agent Display Names tragen ein `Agent`-Suffix; die bisherigen Control-Agent-IDs `nexus` und `nexus-applier` wurden zu `control-router` und `merge-applier` migriert.

Geänderte Grenzen:
- Umgesetzt: Nexus bleibt Projekt-/Authority-Kontext, wird aber nicht als tiefes technisches Prefix-Schema für alle Komponenten eingeführt; Bezeichnungen wie `Nexus Core`, `Nexus Ledger`, `Nexus Blueprint` oder `Nexus Scheduler` sind keine offiziellen aktiven Komponentennamen.
- Umgesetzt: Agents dürfen autoritative Änderungen nicht direkt schreiben; sie arbeiten über `nexusctl` und dessen geprüfte Services.
- Umgesetzt: Die Schedule-/Runtime-Authority-Grenzen sind in Blueprint, Policies, Capabilities, Schedule-Guardrails, Runtime-Tool-Guardrails, generierter OpenClaw Runtime Config und Contract-Tests geprüft beziehungsweise geschärft.

Entfernte oder bereinigte Bestandteile:
- Umgesetzt: Missverständliche aktive Dokumentationssprache rund um Nexus-Konfiguration, generierte Runtime-Artefakte und Agent-Authority wurde entschärft.
- Umgesetzt: missverständliche aktive Benennung wie Agent Display Name `Nexus` wurde durch Agentenbenennung und Authority-Grenzen entschärft; `Merge Applier Agent` bleibt als dedizierte Rolle für gate-konformes Anwenden erhalten.

Neue bekannte Risiken:
- Agent-ID-Migration wurde in P2 umgesetzt; relevant bleiben mögliche externe Token, gespeicherte lokale Entwicklungsdatenbanken oder manuelle Skripte außerhalb dieses Repositorys, die noch die alten Agent-IDs `nexus` oder `nexus-applier` verwenden.
- Zu starke Umbenennung könnte bestehende Validierungs- und Testverträge brechen; deshalb bleiben Projektname `OpenClaw Nexus`, Toolname `nexusctl` und Ordner `nexus/` zunächst stabil.

Empfohlene nächste Arbeiten:
- Sprint mit `clear Sprint` abschließen, damit der LLM-Doublecheck durchgeführt, `.chatgpt/state/CURRENT_STATE.md` aktualisiert, das Sprint-Log archiviert und `.chatgpt/state/phases.md` geleert wird.
- Nach Sprintabschluss optional einen separaten Produkt-Sprint für echte Schedule-Change-Request-Flows planen, falls Agents Cronjob-Erstellung aktiv beantragen und reviewbar machen sollen.


## LLM-Doublecheck

Prüfumfang:
- `.chatgpt/state/phases.md` vollständig gelesen und geprüft: alle Phasen P1 bis P4 sind abgeschlossen, Validierungen und offene Punkte sind dokumentiert, `Current-State-Delta` ist vorhanden.
- Aktive Dokumentation geprüft: `README.md`, `docs/product/overview.md`, `docs/product/local-development.md`, `docs/operations/internal-production.md`.
- Control Config und Runtime-Grenzen geprüft: `nexus/agents.yml`, `nexus/blueprint.yml`, `nexus/policies.yml`, `nexus/capabilities.yml`, `nexus/schedules.yml`, `nexus/runtime-tools.yml`.
- Generator-/Service- und Testflächen stichprobenhaft geprüft: Schedule-Service, OpenClaw-Config-/Schedule-Writer, Validator und relevante Contract-Tests.
- Generierte Artefakte und aktive Agentenbenennung geprüft: `generated/agents/*`, `generated/skills/*`, `generated/openclaw/*`.

Ergebnis:
- Der tatsächliche Ist-Zustand entspricht dem `Current-State-Delta`: Sprachvertrag, Agentenbenennung, Control-Router-/Merge-Applier-Migration und Authority-Grenzen sind aktiv umgesetzt.
- Keine aktive Dokumentation beschreibt einen Agenten als Source of Truth; `nexusctl` bleibt die autoritative Control-Software.
- Keine aktive Zielversion führt `Nexus Core`, `Nexus Ledger`, `Nexus Blueprint` oder `Nexus Scheduler` als offizielles internes Komponentenschema fort.
- Alte Agent-IDs `nexus` und `nexus-applier` sind in aktiver Agent-Konfiguration und generierten Agent-Verzeichnissen nicht mehr maßgeblich; verbleibende zulässige Nexus-Treffer betreffen `nexusctl`, Projekt-/Ordnernamen, Capability-/Tool-IDs oder Archivhistorie.
- Die Schedule-/Runtime-Grenze ist bewusst ohne vollständiges Schedule-Change-Request-Produkt umgesetzt: Agents dürfen Änderungsbedarf formulieren oder erlaubte Runs auslösen, aber keine produktiven Cronjobs oder generierten Schedule-Artefakte direkt mutieren.

Änderungen an `.chatgpt/state/CURRENT_STATE.md`:
- Sprach- und Architekturvertrag als abgeschlossene Fähigkeit ergänzt.
- Agentenbenennung und Migration von `nexus`/`nexus-applier` zu `control-router`/`merge-applier` ergänzt.
- Authority-Grenzen für Config, Schedules und Runtime-Artefakte ergänzt.
- Zuletzt geprüfte Fast-Testzählung auf 44 Tests und 104 abgewählte Tests aktualisiert.
- Bekannte Grenzen um mögliche externe Alt-ID-Referenzen und das bewusst noch nicht vorhandene Schedule-Change-Request-Produkt ergänzt.
- Empfohlene nächste Arbeiten um einen optionalen Schedule-Change-Request-Sprint ergänzt.

Bewusst akzeptierte Abweichungen:
- Externe lokale Datenbanken, Tokens oder Skripte außerhalb des Repositorys können weiterhin alte Agent-IDs referenzieren; das Repository erzwingt keine Altversions-Kompatibilität.
- Projektname `OpenClaw Nexus`, Toolname `nexusctl`, Ordner `nexus/` sowie zulässige Capability-/Tool-IDs mit Nexus-Bezug bleiben stabil.

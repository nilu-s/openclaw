# Sprint-Log

```yaml
sprint:
  schwerpunkt: Offsite Recovery Evidence Pack für internen Produktionsbetrieb
  ziel: >-
    OpenClaw Nexus kann für Backup/Restore und Audit-Integrität ein prüfbares,
    maschinenlesbares Recovery-Evidence-Manifest erzeugen und fehlende Offsite-/Retention-
    Absicherung im internen Produktionsprofil sichtbar machen, ohne produktive Infrastruktur
    oder Secrets im Repository zu erzwingen.
  nicht_ziele:
    - Keine echte Cloud-, WORM-, S3-, GCS-, Azure- oder Betreiberplattform-Integration implementieren.
    - Kein Wechsel weg von SQLite und keine Änderung des autoritativen Control-Store-Modells.
    - Keine produktiven Secrets, Betreiberpfade oder echten Produktionsdaten ins Repository schreiben.
    - Keine Live-Verifikation gegen externe Backup-Systeme oder echte GitHub-/Cloud-Umgebungen.
    - Keine Ausführung von Timeout-Risiko-Tests außerhalb der bestehenden Fast-Validierung.
  validierungsniveau: fast
  ausgabe: openclaw-nexus.zip
  aktuelle_phase: abgeschlossen
```

## P1 — Evidence-Vertrag und Datenmodell schneiden

Ziel:
- Der Recovery-Evidence-Vertrag ist als kleines, testbares internes Modell definiert und kann aus bestehenden Backup-/Restore-Drill-Ergebnissen abgeleitet werden.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `nexusctl/src/nexusctl/app/restore_drill_service.py`
- `nexusctl/src/nexusctl/app/backup_service.py`
- `nexusctl/src/nexusctl/storage/sqlite/backup.py`
- `tests/test_cli_runtime.py`
- `tests/test_storage_sqlite.py`
- `docs/operations/internal-production.md`

Aufgaben:
- Ein kleines Recovery-Evidence-Modell beziehungsweise Payload-Builder einführen, der keine Secrets rendert.
- Felder festlegen und stabil halten: `evidence_version`, `generated_at`, `source_db`, `backup_path`, `backup_checksum`, `schema_version`, `latest_schema_version`, `event_chain_status`, `checked_events`, `doctor_status`, `failed_checks`, `counts`, `restore_drill_status`, `retention_status`, `offsite_status`.
- Aus bestehenden Restore-Drill- und Backup-Check-Ergebnissen ableiten, ob der lokale Recovery-Nachweis grün ist.
- Offsite-/Retention-Felder zunächst bewusst als Betreiberkonfiguration modellieren, nicht als echte externe Infrastrukturprüfung.
- Unit- oder fokussierte Contract-Tests für grünen Evidence-Payload, fehlende Event-Chain und secretfreie Ausgabe ergänzen.

Akzeptanzkriterien:
- Recovery-Evidence ist ein klar abgegrenztes Modell oder Service-Hilfsmodul und nicht lose in CLI-Ausgabe verstreut.
- Der Payload ist vollständig JSON-serialisierbar und enthält keine Secret-Werte.
- Bestehende Backup-/Restore-Drill-Payloads bleiben kompatibel, außer bewusst dokumentierte Ergänzungen.
- Tests prüfen mindestens grünen Payload und einen nicht-grünen Integrity-/Doctor-Fall.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast -- tests/test_storage_sqlite.py tests/test_cli_runtime.py`

Erwartete Änderung am Current-State-Delta:
- Recovery-Evidence-Vertrag wird als neue interne Betriebsfähigkeit beschrieben.
- Bekannte Grenze bleibt: keine echte externe Offsite-Infrastrukturprüfung.

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `nexusctl/src/nexusctl/app/recovery_evidence.py`
  - `tests/test_recovery_evidence.py`
  - `.chatgpt/state/phases.md`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `PYTHONPATH=nexusctl/src pytest -q tests/test_recovery_evidence.py` — bestanden
  - `./scripts/run_tests.sh fast -- tests/test_recovery_evidence.py tests/test_storage_sqlite.py tests/test_cli_runtime.py` — durch äußeres Sandbox-Timeout abgebrochen, ohne sichtbaren Pytest-Fehler
  - `PYTHONPATH=nexusctl/src pytest -q tests/test_storage_sqlite.py tests/test_cli_runtime.py` — durch äußeres Sandbox-Timeout abgebrochen, ohne sichtbaren Pytest-Fehler
- Offene Punkte:
  - CLI-Integration für `restore-drill --json` und Manifestdatei bleibt planmäßig P2.
  - Doctor-Readiness für Offsite-/Retention-/Evidence-Konfiguration bleibt planmäßig P3.

## P2 — CLI- und Restore-Drill-Integration für Evidence-Manifeste

Ziel:
- Betreiber können beim Restore-Drill ein prüfbares Evidence-Manifest erzeugen oder den Evidence-Payload direkt über JSON-Ausgabe erhalten.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `nexusctl/src/nexusctl/interfaces/cli/commands/db.py`
- `nexusctl/src/nexusctl/interfaces/cli/commands/common.py`
- `nexusctl/src/nexusctl/app/restore_drill_service.py`
- `tests/test_cli_runtime.py`

Aufgaben:
- `nexusctl db restore-drill` so erweitern, dass ein Evidence-Payload im JSON-Ergebnis enthalten ist.
- Optionalen CLI-Parameter für Manifest-Ausgabe ergänzen, zum Beispiel `--evidence-path`, falls das bestehende CLI-Design dazu passt.
- Bei Manifest-Ausgabe atomar und secretfrei schreiben; bestehende Zieldateien nur nach bewusster Entscheidung überschreiben oder kontrolliert ablehnen.
- Menschenlesbare CLI-Ausgabe knapp halten und keine Pfad-/Secret-Leaks erzeugen.
- Negative CLI-Tests für defektes Backup, nicht schreibbares Manifest-Ziel oder fehlende Drill-Voraussetzungen ergänzen, soweit ohne instabile Sandbox-Abhängigkeiten möglich.

Akzeptanzkriterien:
- `nexusctl db restore-drill --json` enthält einen maschinenlesbaren Evidence-Abschnitt.
- Ein Manifest kann in eine Datei geschrieben und anschließend als JSON gelesen werden.
- Kaputte Backups oder Event-Chain-Fehler erzeugen nicht-grüne Evidence, ohne Secrets zu rendern.
- Bestehende Smoke-Tests für Backup, Restore und Restore-Drill bleiben grün.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast -- tests/test_cli_runtime.py`

Erwartete Änderung am Current-State-Delta:
- Restore-Drill liefert nicht nur lokalen Drill-Status, sondern auch ein archivierbares Evidence-Manifest.

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `nexusctl/src/nexusctl/app/recovery_evidence.py`
  - `nexusctl/src/nexusctl/app/restore_drill_service.py`
  - `nexusctl/src/nexusctl/interfaces/cli/commands/db.py`
  - `tests/test_cli_runtime.py`
  - `.chatgpt/state/phases.md`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `PYTHONPATH=nexusctl/src pytest -q tests/test_cli_runtime.py::test_cli_db_restore_drill_command_reports_recovery_contract tests/test_cli_runtime.py::test_cli_db_restore_drill_writes_recovery_evidence_manifest tests/test_cli_runtime.py::test_cli_db_restore_drill_rejects_empty_external_backup_without_secret_leak tests/test_cli_runtime.py::test_cli_db_restore_drill_rejects_tampered_event_chain_backup -q -vv` — bestanden, 4 Tests
  - `./scripts/run_tests.sh fast -- tests/test_cli_runtime.py` — durch äußeres Sandbox-Timeout abgebrochen, nachdem 11 von 13 Tests sichtbar durchgelaufen waren; keine sichtbare Assertion- oder Pytest-Fehlermeldung
- Offene Punkte:
  - Doctor-Readiness für Offsite-/Retention-/Evidence-Konfiguration bleibt planmäßig P3.
  - Betriebsdokumentation und Wizard-Vertrag bleiben planmäßig P4.

## P3 — Doctor-Readiness für Offsite, Retention und Recovery-Evidence

Ziel:
- `nexusctl doctor --json` macht im Profil `internal-production` sichtbar, ob Offsite-/Retention-/Evidence-Konfiguration fehlt, unvollständig oder plausibel gesetzt ist.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `nexusctl/src/nexusctl/app/generation_service.py`
- `nexusctl/src/nexusctl/interfaces/cli/commands/doctor.py`
- `tests/test_doctor_reports.py`
- `config/.env.example`
- `scripts/deployment_wizard.py`

Aufgaben:
- Readiness-Einträge für Recovery-Evidence, Offsite-Backup-Konfiguration und Retention-Konfiguration definieren.
- Geeignete Environment-Schalter oder Pfad-/Policy-Variablen verwenden beziehungsweise ergänzen, ohne produktive Provider-Details zu erzwingen.
- Für `development` nicht unnötig blockieren; für `internal-production` fehlende Konfiguration klar als Warnung oder kritisch einstufen.
- Doctor-Ausgabe secretfrei halten und nur Status, Ursache und Operator-Aktion rendern.
- Tests für grüne interne Produktionskonfiguration und fehlende Offsite-/Retention-Konfiguration ergänzen.

Akzeptanzkriterien:
- `operational_readiness` enthält stabile IDs für Offsite-/Retention-/Evidence-Prüfung.
- Interner Produktionsmodus zeigt fehlende Offsite-/Retention-Evidenz sichtbar an.
- Secret- oder Token-Werte erscheinen nicht in Doctor- oder Warning-Ausgaben.
- Existing Doctor-Contracts bleiben stabil oder werden bewusst angepasst.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast -- tests/test_doctor_reports.py`

Erwartete Änderung am Current-State-Delta:
- Interner Produktionsmodus prüft neben lokaler Persistenz auch Recovery-Evidence-/Offsite-/Retention-Betriebsbereitschaft.

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `nexusctl/src/nexusctl/app/generation_service.py`
  - `config/.env.example`
  - `tests/test_doctor_reports.py`
  - `.chatgpt/state/phases.md`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `PYTHONPATH=nexusctl/src pytest -q tests/test_doctor_reports.py::test_doctor_reports_internal_production_readiness_contract -q -vv` — bestanden
  - `PYTHONPATH=nexusctl/src pytest -q tests/test_doctor_reports.py::test_doctor_reports_development_opt_ins_are_not_internal_production_green -q -vv -o faulthandler_timeout=10` — bestanden
  - `PYTHONPATH=nexusctl/src pytest -q tests/test_doctor_reports.py::test_doctor_reports_internal_production_recovery_controls_are_secret_free -q -vv -o faulthandler_timeout=10` — bestanden
  - `./scripts/run_tests.sh fast -- tests/test_doctor_reports.py` — Projektvalidierung bestanden, Pytest mit Exitcode 5 beendet, weil Fast-Modus `integration`/`slow` ausschließt und dadurch alle 9 `test_doctor_reports.py`-Tests deselected wurden
  - Gruppierte Doctor-Report-Pytest-Läufe wurden durch äußeres Sandbox-Timeout abgebrochen; die betroffenen P3-Tests wurden deshalb einzeln fokussiert validiert
- Offene Punkte:
  - Betriebsdokumentation und Wizard-Vertrag bleiben planmäßig P4.

## P4 — Betriebsdokumentation und Deployment-Wizard-Vertrag ergänzen

Ziel:
- Betreiber erhalten einen konkreten, reviewbaren Ablauf für Backup, Offsite-Ablage, Retention, Restore-Drill, Evidence-Archivierung und Incident-Reaktion.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `docs/operations/internal-production.md`
- `docs/operations/deployment-strategy.md`
- `docs/product/local-development.md`
- `scripts/deployment_wizard.py`
- `tests/test_test_strategy.py`
- `tests/test_deployment_wizard.py`

Aufgaben:
- Dokumentation um einen Abschnitt „Recovery Evidence Pack“ ergänzen.
- Minimalen Operator-Vertrag beschreiben: Frequenz von Backups, Restore-Drills, Evidence-Manifest-Archivierung, Offsite-Kopie, Retention und Incident-Schwellen.
- Deployment-Wizard-Bundle so ergänzen, dass neue Environment-Variablen oder Hinweise sichtbar, aber nicht als produktive Secrets fest verdrahtet werden.
- Runbook-Kommandos für `restore-drill --json` und Manifest-Ausgabe ergänzen.
- Doku-/Wizard-Tests aktualisieren, damit der Vertrag gegen Regressionen geschützt ist.

Akzeptanzkriterien:
- Betriebsdoku beschreibt konkrete Operator-Schritte und rote/grüne Kriterien.
- Deployment-Wizard erzeugt weiterhin reviewbare Artefakte ohne echte Secrets.
- Doku-Tests prüfen, dass Restore-Drill, Evidence-Manifest, Offsite und Retention erwähnt werden.
- Keine widersprüchliche Aussage: lokale Backups bleiben lokale Snapshots; externe Absicherung bleibt Betreiberaufgabe.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast -- tests/test_test_strategy.py tests/test_deployment_wizard.py`

Erwartete Änderung am Current-State-Delta:
- Betriebsgrenzen und empfohlene nächste Arbeiten werden präziser: Offsite-/Retention-Vertrag ist dokumentiert, aber externe Infrastruktur bleibt außerhalb des Repositories.

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `docs/operations/internal-production.md`
  - `docs/operations/deployment-strategy.md`
  - `docs/product/local-development.md`
  - `scripts/deployment_wizard.py`
  - `config/docker-compose.yml`
  - `tests/test_test_strategy.py`
  - `tests/test_deployment_wizard.py`
  - `.chatgpt/state/phases.md`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `PYTHONPATH=nexusctl/src pytest -q tests/test_deployment_wizard.py tests/test_test_strategy.py tests/test_docker_runtime.py` — bestanden, 11 Tests
  - `./scripts/run_tests.sh fast -- tests/test_test_strategy.py tests/test_deployment_wizard.py` — bestanden, 6 Tests
- Offene Punkte: keine

## P5 — Sprint-Konsolidierung, Fast-Validierung und State-Delta

Ziel:
- Die Umsetzung ist konsolidiert, Fast-Validierung ist grün oder sauber begründet, und das Sprint-Log enthält einen belastbaren Current-State-Delta für den späteren Sprintabschluss.

Aufwand:
- ca. 4 Stunden für einen erfahrenen Entwickler

Kontext:
- `.chatgpt/state/phases.md`
- `.chatgpt/state/CURRENT_STATE.md`
- `docs/archiv/sprints/`
- `scripts/validate_project.py`
- `scripts/run_tests.sh`
- `scripts/package_project.py`

Aufgaben:
- Geänderte Dateien und Ergebnisse aller Phasen im Sprint-Log nachtragen.
- Einen kompakten `Current-State-Delta` ergänzen: neue Fähigkeiten, Grenzen, Risiken, empfohlene nächste Arbeiten.
- Fast-Validierung ausführen; bekannte Timeout-Risiko-Tests nicht blind einschließen.
- Falls Fast-Validierung in der äußeren Sandbox abbricht, konkrete Teilvalidierung und Timeout-Hinweis dokumentieren.
- `openclaw-nexus.zip` für die Übergabe erzeugen.

Akzeptanzkriterien:
- `.chatgpt/state/phases.md` enthält Status, geänderte Dateien, Validierung und offene Punkte pro ausgeführter Phase.
- Current-State-Delta beschreibt keine unfertigen Zwischenstände als abgeschlossen.
- Fast-Validierung ist grün oder eine externe Timeout-Abweichung ist nachvollziehbar dokumentiert.
- Übergabe-ZIP heißt exakt `openclaw-nexus.zip`.

Validierung:
- `python scripts/validate_project.py`
- `./scripts/run_tests.sh fast`

Erwartete Änderung am Current-State-Delta:
- Abgeschlossener Sprintzustand kann später über `clear Sprint` geprüft, in `CURRENT_STATE.md` übernommen und archiviert werden.

Ergebnis:
- Status: abgeschlossen
- Geänderte Dateien:
  - `.chatgpt/state/phases.md`
- Validierung:
  - `python scripts/validate_project.py` — bestanden
  - `./scripts/run_tests.sh fast` — bestanden, 50 Tests, 105 abgewählt
- Offene Punkte: keine

## Current-State-Delta

Abschlusszustand nach P5:

- Neuer Recovery-Evidence-Vertrag für Backup-/Restore-Drill-Ergebnisse ist als internes Modell und Builder eingeführt.
- Der Evidence-Payload ist JSON-serialisierbar, secretfrei sanitisiert und enthält Offsite-/Retention-Status zunächst als Betreiberkontrollfelder ohne externe Infrastrukturprüfung.
- `nexusctl db restore-drill --json` liefert nun einen maschinenlesbaren `recovery_evidence`-Abschnitt.
- `nexusctl db restore-drill --evidence-path <datei>` kann ein archivierbares Manifest atomar schreiben; bestehende Manifestdateien werden ohne `--overwrite-evidence` kontrolliert abgelehnt.
- Fehlerfälle bei defekten oder manipulationsverdächtigen Backup-Dateien liefern in JSON-Ausgaben nicht-grüne Recovery-Evidence ohne Secret-Leaks.
- `nexusctl doctor --json` enthält im internen Produktionsprofil stabile Readiness-IDs für Recovery-Evidence-Verzeichnis, Backup-Retention-Policy und Offsite-Backup-Control.
- Fehlende Recovery-Evidence-/Retention-/Offsite-Konfiguration wird in `internal-production` als kritische Operational-Readiness angezeigt, bleibt in `development` aber nicht blockierend.
- Doctor-Readiness rendert nur Status, Ursache und Operator-Aktion; konkrete Secret-Werte und Offsite-Ziel-Metadaten werden nicht ausgegeben.
- Betriebsdokumentation beschreibt jetzt einen konkreten Recovery-Evidence-Pack-Vertrag mit Backup-Frequenz, Restore-Drill-Frequenz, Evidence-Manifest-Archivierung, Offsite-Kopie, Retention und Incident-Schwellen.
- Deployment-Strategie und lokale Entwicklungsdoku enthalten Runbook-Kommandos für `restore-drill --json --evidence-path` und grenzen lokale Nachweise von echter Offsite-Absicherung ab.
- Deployment-Wizard erzeugt Review-Werte für Recovery-Evidence, Retention und operator-managed Offsite-Backups, dokumentiert diese im Bundle und mountet Recovery-Evidence als persistentes Ziel in Wizard-/Compose-Verträgen.
- Wizard- und Doku-Tests schützen Restore-Drill-, Evidence-Manifest-, Offsite- und Retention-Hinweise gegen Regressionen.
- Neuer Recovery-Evidence-Vertrag für Backup-/Restore-Drill-Ergebnisse.
- Restore-Drill kann ein archivierbares, secretfreies Evidence-Manifest erzeugen oder im JSON-Report ausgeben.
- Doctor-Readiness macht fehlende Offsite-/Retention-/Evidence-Konfiguration im internen Produktionsprofil sichtbar.
- Betriebsdokumentation beschreibt konkrete Operator-Schritte für Backup, Offsite-Kopie, Retention, Restore-Drill, Evidence-Archivierung und Incident-Reaktion.
- Bekannte Grenze bleibt: Das Repository erzwingt keine echte externe Backup-Infrastruktur, keine WORM-Speicher und keine Cloud-Provider-Integration.


## LLM-Doublecheck

Prüfumfang:
- Root-README, ChatGPT-Workspace-README und Sprint-Workflow-Skill gelesen.
- Aktives Sprint-Log vollständig gegen Code, Tests, aktive Dokumentation, Scripts und Konfiguration gegengeprüft.
- Recovery-Evidence-Implementierung in `nexusctl/src/nexusctl/app/recovery_evidence.py`, Restore-Drill-Integration, CLI-Optionen, Doctor-Readiness, Environment-/Docker-Konfiguration, Deployment-Wizard und Betriebsdokumentation geprüft.
- Altversions-Kompatibilität geprüft: keine alte Zielversion oder alte Agent-ID wird als aktive Produktversion fortgeführt.

Ergebnis:
- Der Current-State-Delta ist mit dem tatsächlichen Repository-Zustand konsistent.
- Die neuen Recovery-Evidence-Fähigkeiten sind implementiert und durch fokussierte sowie Fast-Tests abgedeckt.
- Externe Offsite-/Retention-Infrastruktur bleibt bewusst Betreiberverantwortung und wird nicht als im Repository implementierte Provider-Liveprüfung dargestellt.
- `.chatgpt/state/phases.md` kann archiviert und geleert werden.

Änderungen an `.chatgpt/state/CURRENT_STATE.md`:
- Zusammenfassung um Recovery-Evidence-Härtung ergänzt.
- Umgesetzte Fähigkeiten um Recovery-Evidence-Payload, Manifest-Schreiben, Doctor-Readiness für Recovery-Evidence/Retention/Offsite und Wizard-/Doku-Vertrag ergänzt.
- Validierungsstand auf den letzten Fast-Lauf mit 50 Tests und 105 abgewählten Tests aktualisiert.
- Bekannte Grenzen um den Betreibercharakter von Offsite-/Retention-Metadaten präzisiert.
- Empfohlene nächste Arbeiten um Recovery-Evidence-Fehler-Monitoring präzisiert.

Bewusst akzeptierte Abweichungen:
- Keine Provider-spezifische Offsite-Liveverifikation im Repository; Offsite-Ziel, Upload-Quittungen, Verschlüsselung und Retention-Durchsetzung bleiben Betreiberaufgabe.
- Timeout-Risiko-Tests bleiben quarantänisiert und wurden beim Sprintabschluss nicht blind in die Standardvalidierung aufgenommen.

# GitHub webhook fixtures

These payloads are representative, secret-free GitHub webhook shapes used for
adapter, hardening, and reconciliation contract tests. They are handcrafted from
public GitHub webhook field conventions and must not contain production secrets,
private repository data, personal access tokens, private keys, or real user data.

Fixture naming convention: `<event>_<scenario>.json`.
